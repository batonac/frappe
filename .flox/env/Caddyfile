:{$FRAPPE_WEBSERVER_PORT} {
	root * sites

	# Serve static files
	@static {
		path /assets/* /files/* /private/files/*
	}
	handle @static {
		file_server
	}

	# Proxy to gunicorn
	reverse_proxy localhost:{$FRAPPE_APP_PORT}
}
